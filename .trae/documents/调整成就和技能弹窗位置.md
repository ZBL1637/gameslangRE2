我将修改 `src/pages/BattlePlain/BattlePlain.scss` 文件，调整成就通知和技能通知的 CSS 定位。

具体计划：
1.  将 `.achievement-toast` 的定位从 `bottom: 2rem` 改为 `top: 2rem`，使其固定在屏幕右上角，符合用户习惯且避免遮挡底部内容。
2.  将 `.skill-toast` 的定位从 `bottom: 2rem` 改为 `top: 2rem`，使其固定在屏幕左上角，与成就通知形成对称，并避开屏幕中央和底部的关键信息区域。

这将解决弹窗位置不正确以及可能遮挡其他 UI 元素的问题。