我将再次修改 `src/pages/BattlePlain/BattlePlain.tsx`，以确保章节完成状态被正确记录，并实现“重新开始”功能。之前的修改似乎未生效或被覆盖。

具体计划：
1.  **修复完成逻辑**：在 `handleComplete` 函数中添加 `completeChapter(2)` 调用。这将确保点击“返回世界地图”时，第二章被标记为已完成，从而修复世界地图上的标记问题。
2.  **实现重新开始功能**：
    *   添加 `handleRestart` 函数，用于重置游戏状态（清空碎片、技能解锁状态等）。
    *   修改 `showOutro` 的初始状态，使其在章节已解锁技能（即已完成）时默认为 `true`，这样再次进入章节时会直接显示完成页。
    *   将 `handleRestart` 传递给 `OutroSection` 组件，使其显示“重新开始本章”按钮。

这样将解决用户提出的两个问题：世界地图标记不正确，以及缺少重新开始功能。